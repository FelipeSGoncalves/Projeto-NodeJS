<h2>Lista de Categorias</h2>
<hr>

<div class="card">
    <div class="list-group list-group-flush">
        {{#each categorias}}
        <a href="/categorias/posts/{{slug}}">
            <li class="list-group-item">{{nome}}</li>
        </a>
        {{else}}
        <h3 class="mt-3"> Nenhuma categoria registrada </h3>
        {{/each}}
        </ul>
    </div>
</div>
